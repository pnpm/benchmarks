npm-debug.log

node_modules

# every scratch directory a run makes: `.tmp`, and the `TMPDIR` a local
# verification or populate is pointed at (`.tmp-populate`, `.tmp-verify`, ...)
.tmp*

managers

versions.json

samples
.claude

.pnpr-cache
